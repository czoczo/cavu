---
title: Repo content
description: Git repository content/structure description.
tableOfContents: false
sidebar:
  order: 0
---

import srcCfgMain from '../../../../../config/main.yaml?raw';
import srcCfgItems from '../../../../../config/items.yaml?raw';

import srcAvatars from '../../../../../avatars.go?raw';
import srcBranding from '../../../../../customisation.go?raw';
import srcConfig from '../../../../../config.go?raw';
import srcController from '../../../../../controller.go?raw';
import srcFileHandling from '../../../../../file_handling.go?raw';
import srcGraphics from '../../../../../graphics.go?raw';
import srcHttpServer from '../../../../../http_server.go?raw';
import srcIconCrawl from '../../../../../icon_crawl.go?raw';
import srcIconExtraction from '../../../../../icon_extraction.go?raw';
import srcKubernetes from '../../../../../kubernetes.go?raw';
import srcMain from '../../../../../main.go?raw';
import srcModels from '../../../../../models.go?raw';
import srcParsingHelpers from '../../../../../parsing_helpers.go?raw';
import srcStatuses from '../../../../../statuses.go?raw';

import { Aside, Code, Tabs, TabItem } from '@astrojs/starlight/components';
import { FileTree } from '@astrojs/starlight/components';

---

It can be useful to familiarise yourself with the contents of a project's Git repository before starting work.

[comment]: # (ls --group-directories-first -A1)
<FileTree>
- config/ // default configuration definitions
  - main.yaml {srcCfgMain.slice(0, srcCfgMain.indexOf("\n"))}
  - items.yaml {srcCfgItems.slice(0, srcCfgItems.indexOf("\n"))}
- deployment/ ways to deploy CasaVue
  - docker-compose/ 
  - helm/ application Helm Chart
- frontend/ GUI ([Vue](https://vuejs.org/)) source files for CasaVue
- pages/ **this** docs - a standard [Starlight](https://starlight.astro.build/) project
- avatars.go {srcAvatars.slice(0, srcAvatars.indexOf("\n"))}
- customisation.go {srcBranding.slice(0, srcBranding.indexOf("\n"))}
- config_generator.py
- config.go {srcConfig.slice(0, srcConfig.indexOf("\n"))}
- controller.go {srcController.slice(0, srcController.indexOf("\n"))}
- Dockerfile
- file_handling.go {srcFileHandling.slice(0, srcFileHandling.indexOf("\n"))}
- graphics.go {srcGraphics.slice(0, srcGraphics.indexOf("\n"))}
- http_server.go {srcHttpServer.slice(0, srcHttpServer.indexOf("\n"))}
- icon_crawl.go {srcIconCrawl.slice(0, srcIconCrawl.indexOf("\n"))}
- icon_extraction.go {srcIconExtraction.slice(0, srcIconExtraction.indexOf("\n"))}
- kubernetes.go {srcKubernetes.slice(0, srcKubernetes.indexOf("\n"))}
- **main.go** {srcMain.slice(0, srcMain.indexOf("\n"))}
- models.go {srcModels.slice(0, srcModels.indexOf("\n"))}
- parsing_helpers.go {srcParsingHelpers.slice(0, srcParsingHelpers.indexOf("\n"))}
- README.md
- statuses.go {srcStatuses.slice(0, srcStatuses.indexOf("\n"))}
</FileTree>
